{
  "name": "demo01",
  "version": "1.0.0",
  "description": "demo 01",
  "main": "main.js",
  "scripts": {
	"build": "tsc -w",
	"serve": "serve ./dist",
	"build_doc": "typedoc --tsconfig tsconfig.json --out doc ./x4/src/*.ts src/*.ts"
  }
}
